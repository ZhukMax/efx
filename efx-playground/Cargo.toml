[package]
name = "efx-playground"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.33.0"
egui = "0.33.0"
efx = "0.6.1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = "z"
panic = "abort"
